# typed: true

module Orange
  class Cmd
    class Project
      # Refresh the project status, calling the appropriate core flow
      # TODO: Define the core flow here
      #
      #   Cmd::Project::RefreshStatus.new(project.id)
      class RefreshStatus < Project
      end
    end
  end
end
